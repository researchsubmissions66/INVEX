name: invex
channels:
  - conda-forge
dependencies:
  - python=3.10
  - numpy=2.2.6
  - scipy=1.15.3
  - pandas=2.2.3
  - scikit-learn=1.7.2
  - h5py=3.16.0
  - matplotlib=3.10.9
  - pillow=12.2.0
  # GPU stage only (rotated re-embedding); install separately to match your CUDA:
  #   pip install torch==2.5.1 timm==0.9.16
